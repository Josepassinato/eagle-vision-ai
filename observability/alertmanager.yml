global:
  smtp_smarthost: 'localhost:587'
  smtp_from: 'alertmanager@visaoaguia.com'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'web.hook'

receivers:
  - name: 'web.hook'
    webhook_configs:
      - url: 'http://notifier:8080/alerts'
        send_resolved: true

  - name: 'telegram'
    webhook_configs:
      - url: 'http://notifier:8080/telegram'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'dev', 'instance']