import { SimpleCameraOnboarding } from '@/components/SimpleCameraOnboarding';

const CameraSetup = () => {
  return (
    <div className="container mx-auto p-6">
      <SimpleCameraOnboarding />
    </div>
  );
};

export default CameraSetup;
